const {
  diskUsage,
  getAll,
  getSerialNumber,
  isConnectedToInternet,
  isProcessRunning,
  restart,
  shutdown,
} = require('./lib/lib');

module.exports = {
  diskUsage,
  getAll,
  getSerialNumber,
  isConnectedToInternet,
  isProcessRunning,
  restart,
  shutdown,
};
